fastapi>=0.128.0
uvicorn[standard]>=0.38.0
yt-dlp>=2025.12.8
python-multipart>=0.0.20
pytest>=9.0.0
httpx>=0.28.1
playwright>=1.56.0
argon2-cffi>=23.1.0
stripe>=13.0.0
# Optional: install f2 separately on Python versions it supports to enable the primary Douyin resolver.
# Optional local speech-to-text: pip install faster-whisper when AI_TRANSCRIBE_PROVIDER=local-faster-whisper.
